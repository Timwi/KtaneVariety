namespace Variety
{
    public enum SwitchColor
    {
        // These must match the order in SwitchPrefab.SwitchMaterials (Material[])
        Blue,
        Red,
        Yellow,
        White
    }
}

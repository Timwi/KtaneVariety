namespace Variety
{
    public enum ButtonColor
    {
        // These must match the order in ButtonPrefab.Colors (Material[])
        RedButton,
        YellowButton,
        BlueButton,
        WhiteButton
    }
}
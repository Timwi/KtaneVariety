namespace Variety
{
    public enum WireColor
    {
        // These must match the order in WirePrefab.WireMaterials (Material[])
        BlackWire,
        BlueWire,
        RedWire,
        YellowWire,
        WhiteWire
    }
}
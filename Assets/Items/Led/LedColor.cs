namespace Variety
{
    public enum LedColor
    {
        // These must match the order in LedPrefab.LedColors (Material[])
        Black,
        Red,
        Yellow,
        Blue,
        White
    }
}
